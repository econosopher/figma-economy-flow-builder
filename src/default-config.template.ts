/**
 * Default configuration template
 * 
 * SETUP INSTRUCTIONS:
 * 1. Copy this file to 'default-config.ts'
 * 2. Add your API key to the copied file
 * 3. NEVER commit default-config.ts to git (it's in .gitignore)
 * 
 * For production builds, leave apiKey empty.
 * For development, you can add your personal API key.
 */

export const DEFAULT_CONFIG = {
  apiKey: '', // Add your API key here for development
  validated: false
};